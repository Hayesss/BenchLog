/**
 * Placeholder — the backend graft owns and overwrites this file in Phase 5
 * (full-screen page, rendered WITHOUT the AppShell layout).
 */
export default function Login() {
  return (
    <div className="flex min-h-[100dvh] items-center justify-center bg-paper">
      <h1 className="font-display text-[30px] font-bold text-ink">Login</h1>
    </div>
  )
}
